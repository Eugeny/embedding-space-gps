# build artifacts / large downloads (re-fetchable; not needed to serve the site)
glove50.gz
build.log
.claude/
.DS_Store

# large raw data — committed gzipped as vectors.json.gz instead (Pages deploy chokes on 46MB)
vectors.json
